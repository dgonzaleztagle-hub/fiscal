"use client";import Link from"next/link";import{useState}from"react";import{ArrowLeft,FileSpreadsheet}from"lucide-react";
export function BankImport(){const[rows,setRows]=useState<string[][]>([]),[saved,setSaved]=useState(false);function load(file:File){file.text().then(text=>{setSaved(false);setRows(text.split(/\r?\n/).filter(Boolean).slice(0,21).map(line=>line.split(/[;,]/)))})}async function save(){const response=await fetch("/api/demo/activities?area=bank_import",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({rows})});setSaved(response.ok)}return <div className="page section-page"><Link className="back-link" href="/caja"><ArrowLeft size={15}/> Volver</Link><header className="page-header"><div><p className="eyebrow">Conciliación manual</p><h1>Importar cartola</h1><p>Vista previa local; nada se confirma sin revisar columnas y totales.</p></div></header><section className="panel import-card"><FileSpreadsheet size={28}/><label>Seleccionar CSV<input accept=".csv,text/csv" type="file" onChange={e=>e.target.files?.[0]&&load(e.target.files[0])}/></label>{rows.length>0&&<><strong>{rows.length-1} movimientos detectados en la muestra</strong><div className="import-preview">{rows.slice(0,6).map((row,i)=><code key={i}>{row.join(" · ")}</code>)}</div><button className="primary-button" onClick={save}>Guardar importación demo</button>{saved&&<div className="validation-success" role="status"><strong>Cartola persistida en el backend sandbox</strong></div>}</>}</section></div>}
