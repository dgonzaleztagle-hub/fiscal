"use client";import{useState}from"react";import{CheckCircle2}from"lucide-react";export function StockControl(){const[product,setProduct]=useState(""),[from,setFrom]=useState("main"),[to,setTo]=useState("bodega"),[quantity,setQuantity]=useState(0),[minimum,setMinimum]=useState(0),[saved,setSaved]=useState("");async function send(kind:"transfer"|"minimum"){const url=kind==="transfer"?"/api/inventory/transfers":"/api/inventory/minimums",body=kind==="transfer"?{product_id:product,from_branch_id:from,to_branch_id:to,quantity}:{product_id:product,branch_id:from,minimum_quantity:minimum};const response=await fetch(url,{method:kind==="transfer"?"POST":"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(response.ok||response.status===503){if(response.status===503)sessionStorage.setItem(`completo-fiscal:stock:${Date.now()}`,JSON.stringify(body));setSaved(kind)}}return <div className="page section-page"><header className="page-header"><div><p className="eyebrow">Control de existencias</p><h1>Mínimos y traslados</h1><p>Un traslado siempre registra ambos lados; nunca ajustes manualmente dos sucursales.</p></div></header><div className="wizard-grid"><section className="panel wizard-form"><h2>Trasladar stock</h2><label>ID producto<input value={product} onChange={e=>setProduct(e.target.value)}/></label><label>Desde<input value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Hacia<input value={to} onChange={e=>setTo(e.target.value)}/></label><label>Cantidad<input type="number" min="0.001" value={quantity||""} onChange={e=>setQuantity(Number(e.target.value))}/></label><button className="primary-button" onClick={()=>send("transfer")}>Registrar traslado pareado</button></section><section className="panel wizard-form"><h2>Alerta mínima</h2><label>Sucursal<input value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Cantidad mínima<input type="number" min="0" value={minimum||""} onChange={e=>setMinimum(Number(e.target.value))}/></label><button className="secondary-button" onClick={()=>send("minimum")}>Guardar mínimo</button>{saved&&<div className="validation-success" role="status"><CheckCircle2/><div><strong>Control guardado</strong><p>{saved==="transfer"?"Entrada y salida quedaron vinculadas.":"La alerta se calculará contra el saldo real."}</p></div></div>}</section></div></div>}
